import { MODELS } from "./actions";

const VISIBILITY_FILTER = {
  SHOW_ALL: "SHOW_ALL",
  SHOW_COMPLETED: "SHOW_COMPLETED",
  SHOW_ACTIVE: "SHOW_ACTIVE"
};

export default { MODELS, VISIBILITY_FILTER };
